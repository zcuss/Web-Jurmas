<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logs Stok</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Menambahkan sedikit padding dan alignment untuk kolom */
        .table td, .table th {
            vertical-align: middle;
            text-align: left;
        }

        /* Styling untuk tanggal dan jam */
        .log-date-time {
            font-size: 0.9rem;
            color: #555;
            font-weight: bold;
        }

        /* Styling untuk tombol kustom */
        .btn-custom {
            background-color: #007bff;
            color: white;
            font-weight: bold;
            transition: background-color 0.3s ease;
        }

        /* Hover effect untuk tombol */
        .btn-custom:hover {
            background-color: #0056b3;
        }

        /* Menambahkan margin antara tombol dan tabel */
        .btn-container {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <!-- SIDEBAR DI LUAR BODY -->
    <%- include('../-partials/sidebar', { currentRoute: currentRoute }) %>

    <div class="main-content container mt-4">
        <h1 class="mb-4 text-center text-success">📦 Logs Restock</h1>
        <% if (logs.length === 0) { %>
            <p class="text-center text-muted">Belum ada log Restock.</p>
        <% } else { %>
        <% logs.forEach(log => { %>
            <div class="card mb-4">
                <div class="card-body">
                    <table class="table table-bordered table-striped">
                        <thead class="thead-dark">
                            <tr>
                                <th>Nama Bahan</th>
                                <th>Jumlah</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% log.items.forEach(item => { %>
                                <tr>
                                    <td><%= item.name %></td>
                                    <td><%= item.quantity %></td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>

                    <!-- Tanggal dan Jam berada di bawah tabel, disesuaikan dengan left-align -->
                    <div class="log-date-time text-left mt-2">
                        <div><strong>Tanggal:</strong> <%= log.date %></div>
                        <div><strong>Jam:</strong> <%= log.time %></div>
                    </div>
                </div>
            </div>
            <% }); %>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
